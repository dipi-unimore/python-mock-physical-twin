
def source_identifier(identifier: str) -> str:
    return f"source.{identifier}"

def destination_identifier(identifier: str) -> str:
    return f"destination.{identifier}"

def device_identifier(identifier: str) -> str:
    return f"device.{identifier}"

def actuator_identifier(identifier: str) -> str:
    return f"actuator.{identifier}"